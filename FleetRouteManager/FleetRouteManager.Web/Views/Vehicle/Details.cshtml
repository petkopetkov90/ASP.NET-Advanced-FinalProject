@* @model VehicleDetailsViewModel

@{
    ViewData["Title"] = "Details";
}
<div class="text center" style="width: 100%; margin: 0;">
    <h1>
        @Model.RegistrationNumber
    </h1>
    <table class="table table-striped table-hover table-borderless">
        <thead style="font-style: italic">
            <tr>
                <th scope="col">VIN</th>
                <th scope="col">Manufacturer</th>
                <th scope="col">Model</th>
                <th scope="col">First Registration</th>
                <th scope="col">Euro class</th>
                <th scope="col">Type</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td style="padding: 15px">@Model.Vin</td>
                <td style="padding: 15px">@Model.Manufacturer</td>
                <td style="padding: 15px">@Model.Model</td>
                <td style="padding: 15px">@Model.FirstRegistrationDate</td>
                <td style="padding: 15px">@Model.EuroClass</td>
                <td style="padding: 15px">@Model.Type</td>
            </tr>
            <tr>
                <td colspan="6">
                    <table class="table" style="text-align: center; width: 100%">
                        <thead style="font-style: italic">
                            <tr>
                                <th scope="col">Truck Type</th>
                                <th scope="col">Body Type</th>
                                <th scope="col">Axles</th>
                                <th scope="col">Capacity</th>
                                <th scope="col">Acquired on</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td style="padding: 15px">@Model.TruckType</td>
                                <td style="padding: 15px">@Model.BodyType</td>
                                <td style="padding: 15px">@Model.Axles</td>
                                <td style="padding: 15px">@($"{Model.WeightCapacity:f2}t")</td>
                                <td style="padding: 15px">@Model.AcquiredOn</td>
                            </tr>
                        </tbody>
                    </table>
                </td>
            </tr>
        </tbody>
        <tfoot>
            <tr>
                <td style="padding: 5px 0px">
                   @if (User.IsInRole("Admin"))
                    {
                    <a asp-action="Add" asp-controller="Vehicle" style="border-radius: 9999px; background-color:forestgreen; border-color: forestgreen" class="btn btn-primary">Add new Vehicle</a>
                 }
                </td>
            </tr>
        </tfoot>
    </table>
</div> *@